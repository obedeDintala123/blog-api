version: "3.8"

services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=blog
      - POSTGRES_PASSWORD=blog
      - POSTGRES_DB=blogDB
    
    volumes:
      - blogDB_pg_data:/bitnami/postgresql

volumes:
  blogDB_pg_data:
